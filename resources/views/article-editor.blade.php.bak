@extends('layouts.app')
@section('title', 'Редактор событии')
@section('content')
<a href="{{ route('event-create')}}">Создать событие</a>
<table class="table table-striped table-borderless">
	<thead>
		<tr>
			<th>Название</th>
			<th>Адрес</th>
			<th>Дата</th>
			<th>&nbsp;</th>
		</tr>
	</thead>
	<tbody>
		@foreach ($events as $event)
		<tr>
			<td><h4>{{$event->title}}</h4></td>
			<td>{{$event->address}}</td>
			<td>{{$event->date}}</td>
			<td>
				<a href="{{ route('event-edit', ['event' => $event]) }}">Редактировать</a>
			</td>
			<td>
				<form action="{{ route('event-delete', ['event' => $event]) }}" method="post">
					@csrf
					@method('DELETE')
					<input type="submit" value="Удалить">
				</form>
			</td>
		</tr>
		@endforeach
	</tbody>
</table>
{{ $events->links('pagination::bootstrap-4') }}
@endsection('content')